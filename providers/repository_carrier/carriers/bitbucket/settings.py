
identity = {
        'type': 'repository_carrier',
        'name': 'bitbucket'
}

# Full path to local repository directory where bitbucket repositories are cloned.
local_repo_dir = '/full/path/to/repo/bitbucket'

# Bitbucket credentials.
creds = {
    'userfullname': 'BITBUCKET USER NAME',
    'username': 'BITBUCKET USER NAME',
    'userpasswd': 'BITBUCKET USER PASSWORD',
    'useremail': 'BITBUCKET USER EMAIL ADDRESS'
}

kafka = {
    'topic': 'bitbucket'
}
